image: node:22 # Встановлюємо образ для Node.js (можеш змінити версію за потреби)

pipelines:
  default:
    - step:
        name: Run Tests and Lint
        caches:
          - node
        script:
          - npm install
